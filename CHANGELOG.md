# 更新日志

每次发布都必须同步更新本文件，以及 `index.js` 中酒馆内可见的 `RELEASE_HISTORY`。

## 2.5.1 - 2026-08-26

- 插件自动维护的角色档案与当前场景世界书条目恢复为正常启用。升级后会直接重新启用现有条目，不删除资料、不重新搜索；即使插件临时停用，已保存的条目仍可由酒馆原生世界书按角色卡和关键词继续注入。
- 正常生成前增加纯本地世界书激活预检：内容与本地数据库完全一致、且本轮已由酒馆激活的插件条目会从扩展提示中去重，只补充未激活、尚未落盘、保存待修复或超出世界书预算的当前相关资料。
- 当前场景条目保持角色卡过滤下的常驻启用；人物、地点、物品、能力、组织、事件与世界规则档案继续按关键词选择性启用，避免把整库资料同时塞入上下文。
- 世界书预检只运行酒馆本地匹配，不调用分析模型和搜索 API；持久条目与本地补充共同覆盖原有生成前核验能力，不新增 429 来源。

## 2.5.0 - 2026-08-26

- 原著资料库升级为通用实体模型：人物、地点、物品、能力、组织、事件和世界规则均可独立识别、检索、建档、注入与审核，不再把地点强制当原创，也不再只研究人物。
- 正常正文生成前改为纯本地选取当前场景、本轮点名与手动固定对象，直接注入已核实的紧凑原著基线和当前有效 AU；不发分析或搜索请求，因此不增加首字等待与 429。
- AU 升级为结构化当前事实：按“唯一归属对象 + 细粒度属性”更新，同属性新状态会替换旧状态；旧状态完整保留在历史中但不再注入。关系句不会因提到多人而串入每个人档案。
- 旧版全局字符串与人物条目会自动迁移并按句首唯一归属重新分配，可自动清理“彼得的变化写进斯特兰奇档案”一类存量污染。
- 原著基线与本卡 AU 完全分层，档案压缩不再把 AU 写进原著资料；时间线明确跨节点时只把相关对象标记待更新，并在后台增量核实。
- 生成后审核改为先应用最小事实修订，再按最终正文写入场景和 AU；一次疑似 OOC 的助手静态断言不能被当作 AU，只有带明确动作与结果的实际状态变化才可入库。
- 审核覆盖正式姓名、外貌、性格、经历、关系、地点、物品归属与功能、能力机制与限制、组织、事件和世界规则；同一错名重复出现时可安全一次修正全部。
- `currentEntities` 与 `canonSubjects` 分离：本轮出现后又离场的原作对象仍会建档审核，但不会错误留在当前场景。
- 搜索结果按候选实体、类型和所属作品严格配对；自定义搜索 AI 必须回填准确 `candidateId`、作品身份和逐对象引用才能通过。严格模式只采用基线与来源信任都已验证的档案，旧版 provisional 结果不能经迁移、别名合并或压缩被洗白。
- 单轮实际查询对象严格不超过“每次最多查询数”，超出的对象保留为待补并在后续剧情轮次增量处理；失败使用有界退避记录，不再在一轮中连续发送多批请求。
- 自动后台场景同步、AU 更新与生成后审校只在选择并配置独立分析 API 时运行；当前酒馆模型只供明确点击的手动操作，正常消息事件和后台巡检不会调用它。
- 快速重 Roll、停止、切换 swipe 与编辑消息只用于按“聊天 + 消息”作废过时任务；插件绝不会点击、触发或排队重 Roll。停止后的半截流式正文不会审核、写 AU 或世界书，已有后续消息时也不会回头修改旧回复。
- 分析与 Chat Completions 搜索的 POST 固定经酒馆同源通用代理单路径发送，不再浏览器直连后重复 POST；HTTP 429 与上游 5xx 仅作有限重试并尊重 `Retry-After`，鉴权和请求错误不重试。
- 世界书保存后会清除缓存并从磁盘复读 marker/hash；只有复核一致才显示同步成功。群聊固定使用同一本资料书并允许所有群成员读取，重置会遍历全部曾写入的世界书。
- 世界书加载异常不再被误判为用户手删；只有曾成功落盘且当前确实缺失的对象条目才会删除本地档案，删除一个人物也不会按全文子串误删其他对象的 AU。
- 世界书 AU 证据绑定具体书名、条目 UID 与内容版本：启用但本轮未触发的条目继续有效；真正关闭、删除或改掉依据的条目会在下一次生成前撤回旧 AU，并同步清理插件世界书。
- 用户明确写出“推进/切换到某事件、篇章或阶段”时，生成前纯本地注入立即采用该节点；生成后同样以用户原句更新表格。回忆、档案、录像、梦境和假设中的年份/事件不会反向覆盖当前时间线。
- 自定义搜索 AI 的严格信任只计算服务端 `web_search` annotations/citations；模型在 JSON 正文中自行编写两个网址也不能把资料升级为 verified。
- 长角色卡和启用世界书改为首尾、明确设定窗口及均匀位置联合抽取；单对象档案标题后的外貌、性格、经历分段会完整保留，不再只剩姓名标题。
- 新对象缺少 `isOriginal` 时按未知/原创安全处理，仅保留场景显示，不外搜；AU owner 必须绑定本轮实际对象或既存同作品档案，普通句子不能凭空创建人物与状态。
- 自动改文除正式姓名外必须把原片段与对应档案实体绑定在同一句中，并限制为原作事实属性；人物标签不能再被用来修改无关动作、剧情结果或相邻句。
- 手动核验选择当前酒馆模型时，未结束前会明确阻止新的正文生成，避免无法真正取消的旧请求与主模型并发、产生延迟 429 或把半截正文写入；停止后 swipe/edit 的有限重试会保留允许状态。

## 2.4.2 - 2026-08-23

- 修复生成后场景分析虽然能更新人物、地点和时间线，却把 AU 差异硬编码为空数组并直接丢弃的问题。
- AU 差异改为通用增量记录：身份、年龄、阵营、能力、装备与关键物品、外貌与身体状态、经历与记忆、生死去向、人际关系、地点势力、事件结果、人物认知、世界规则和时间线等，只要有明确依据且影响后续写作都可写入，不局限于固定类别。
- 差异同时保存到本局 AU 总表、对应人物条目和当前场景世界书；即使差异属于组织、物品或世界规则而没有独立人物档案，也不会丢失。
- 每次正文生成前会从本地资料直接注入已确认 AU 差异，确保正文实际采用；该步骤不调用分析或搜索 API，不增加额外 429。
- 正常生成、立即检索和手动核验统一读取本局 AU 总表，属于物品或世界规则的差异不会因切换入口而漏掉。
- 严格区分“已明确与原著不同”和“正文暂时未提到”：只接受角色卡、用户明确设定、当前启用世界书、反复建立的上下文或本轮明确发生的变化。

## 2.4.1 - 2026-08-21

- 生成后审核优先对照用户最近明确写出的年份、年初/年底、季节或月份，仅修正正文中相互矛盾的时间短语。
- 明确的用户时间锚点同时约束当前场景和世界书，防止正文中的模糊时间反向污染资料库。
- 时间核对只做本地文本比对，不增加搜索、分析请求或生成前等待。

## 2.4.0 - 2026-08-20

- 修复生成后自动总结偶尔完全不触发的问题：消息接收、渲染完成和生成结束统一进入去重队列，后台巡检会自动补跑遗漏。
- 新增每个聊天独立的“当前场景”常驻世界书条目，自动同步作品、时间线、在场人物、地点及已发生状态；角色基础档案仍按人物独立保存。
- 场景分析或世界书写入失败时自动按 5、15、45 秒退避重试，设置页会持久显示同步中、已同步或具体错误。
- 刷新、升级或切换聊天后会检查最后一条完整助手正文，自动修复旧版本漏掉的场景同步。
- 新原作角色检索并建立档案后自动复核刚生成的正文，补上首次登场时可能漏掉的姓名、外貌及 OOC 修订。
- 世界书写入改为串行队列，避免场景快照与角色档案同时保存时互相覆盖。
- 时间线明确切换时，已有角色档案的“当前剧情线”标题会同步刷新，避免和当前场景条目产生矛盾。
- 正文含 `NE-BANNER` 场景横幅时，人物、地点和时段采用确定性覆盖，防止分析模型把上一场景人物错误保留到新场景。
- 自动保护用户/原创人物不被旧原作档案的错误别名映射，并清理这类污染别名后同步修正世界书关键词。

## 2.3.9 - 2026-08-18

- “AI识别并填写”改用同一套当前场景快照逻辑，替换旧自动项时会保留用户手动固定项。
- 手动“立即检索”现在只核验和补充资料，不再把被提及但未在场的人物加入当前人物表。

## 2.3.8 - 2026-08-18

- 当前人物/地点改为场景快照：自动项会随角色进场、离场和地点切换增减，不再永久累加。
- 用户手动添加的人物或地点继续保留；离场角色的历史档案仍留在本卡资料库，回归时可以直接复用。
- 作品、时间线和时间节点只在正文明确切换时更新，不允许分析模型自行推进剧情。
- 场景状态更新与生成后 OOC 审核合并为一次异步分析请求，不阻塞正文，也不额外重复调用分析 API。

## 2.3.7 - 2026-08-18

- 正常生成改为立即放行，不再在正文生成前调用分析或搜索 API，避免插件导致等待、空回复或额外 429。
- 手动核验只识别用户最新输入中明确点名的对象，不再从角色卡、世界书或历史剧情推测下一位登场角色。
- 生成后审核只检查正文中实际出现的角色，并严格禁止改变剧情走向、登场角色、行动结果和场景顺序。
- 自动修订仅允许替换最短冲突片段，并增加长度与扩写限制，防止审核模型整段重写正文。
- 酒馆内新增本版更新日志：升级后自动显示一次，并可随时在插件设置页顶部查看。
